/****
Contains Tile Generator functions
****/

/*
tileJSON format:

	tiles = {
	

	}
*/

$(document).ready(function(){
	
	var wrapperTileCount = 0 ;	//Count for no of tiles in a tile-wrapper

	//Stringy tileJSON


});

// One tile-wrapper contains three tiles
function generateTileWrapper(){
	
	var scrolling = $('#scrolling').html();
	
	var tile_wrapper = '<div class="tile-wrapper">';
	var tile_wrapper_end = '</div>';
}

function generateTile(tileJSON, tileId){

	var tile = '<div class="tile" id="' + tileId + '">';
	var tile_end = '</div><br>';

}

